"""
Документный движок Dalel.

Один механизм на все виды порождаемых документов: договоры, законопроекты,
а в дальнейшем — процессуальные бумаги. Раньше каждый модуль решал одну и ту
же задачу по-своему, и трижды написанное расходилось в мелочах, из которых
и складывается качество юридического документа.

Основные части:
    schema     дерево документа: разделы, пункты, реквизиты, таблицы
    passport   паспорт типа: поля формы, разделы, существенные условия
    skeleton   каркас документа без обращения к модели
    generate   генерация по разделам и правка указанием
    ops        правки как операции над деревом
    check      проверка состава: существенные условия, реквизиты
    render     HTML, DOCX, PDF, XLSX из одного дерева
    jobs       очередь фоновых задач
"""

from .ops import Op, apply_ops, diff  # noqa: F401
from .passport import get_passport, list_passports, load_catalog  # noqa: F401
from .schema import DocTree, renumber  # noqa: F401

__all__ = [
    'DocTree', 'Op', 'apply_ops', 'diff', 'renumber',
    'get_passport', 'list_passports', 'load_catalog',
]
